[ ] pull the stytem prompt out into its own file that can be generated.
[ ] We have multiple paths to test: regex process each line, LLM process each line, LLM proces whole thing, regex process whohile thing, LLM process whole thing, etc.
